{
  "name": "fantasy-draft-wizard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "fetch:rankings": "tsx scripts/fetch-rankings.ts",
    "build:pool": "tsx scripts/build-pool.ts",
    "update:rankings": "npm run fetch:rankings && npm run build:pool",
    "seed:rankings": "tsx scripts/seed-rankings.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
